namespace Avelia.E2E

open Xunit

module Placeholder =

    /// Placeholder E2E test. Real UI automation tests will replace this once
    /// the shell exposes navigable surfaces and a driver harness is in place.
    [<Fact>]
    [<Trait("Category", "E2E")>]
    let ``E2E harness is reachable`` () = Assert.True(true)
